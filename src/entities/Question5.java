package entities;

public class Question5 {
    /*
    classes que não podem ser instanciadas
    diretamente e servem como base para outras
    classes. Elas podem conter métodos abstratos
    (que são declarados, mas não implementados) e
    métodos concretos (que possuem implementação).
     */
}
