package entities;

public class Question3 {

    /*
        import java.util.ArrayList;

        public abstract class Produto {
            private String nome;
            private double preco;

            public Produto(String nome, double preco) {
                this.nome = nome;
                this.preco = preco;
            }

            public String getNome() {
                return nome;
            }

            public double getPreco() {
                return preco;
            }

            public abstract void mostrarDetalhes();
        }

        public class ProdutoPerecivel extends Produto {
            private String dataValidade;

            public ProdutoPerecivel(String nome, double preco, String dataValidade) {
                super(nome, preco);
                this.dataValidade = dataValidade;
            }

            @Override
            public void mostrarDetalhes() {
                System.out.println("Nome: " + getNome() + ", Preço: " + getPreco() + ", Validade: " + dataValidade);
            }
        }

        public class ProdutoNaoPerecivel extends Produto {
            private String descricao;

            public ProdutoNaoPerecivel(String nome, double preco, String descricao) {
                super(nome, preco);
                this.descricao = descricao;
            }

            @Override
            public void mostrarDetalhes() {
                System.out.println("Nome: " + getNome() + ", Preço: " + getPreco() + ", Descrição: " + descricao);
            }
        }

        public class Main {
            public static void main(String[] args) {
                ArrayList<Produto> listaDeProdutos = new ArrayList<>();

                listaDeProdutos.add(new ProdutoPerecivel("Leite", 5.99, "20/10/2024"));
                listaDeProdutos.add(new ProdutoNaoPerecivel("Arroz", 20.00, "Pacote de 5kg"));

                for (Produto produto : listaDeProdutos) {
                    produto.mostrarDetalhes();
                }
            }
        }

    */
}
